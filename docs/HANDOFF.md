# Handoff

Read `README.md`, `docs/STATE.md`, `docs/deployment.md`, the v2 manifest, and fresh Graphify output before changing release facts.

Draft PR 1 contains the isolated v2 migration and targets the existing `codex/flask-legal-workspace` branch so its prior 20 commits remain separate from this contract change. Next action: review its checks, then request an exact-SHA preview before updating the portfolio registry. Preserve the currently approved legacy SHA until the replacement is reviewed.

Do not promote root HTTP reachability or retrieval/provenance metrics into an availability, confidentiality, legal-accuracy, or production-readiness claim.
