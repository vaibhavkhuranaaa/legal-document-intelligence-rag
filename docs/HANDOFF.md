# Handoff

Read `README.md`, `docs/STATE.md`, `docs/deployment.md`, the v2 manifest, and fresh Graphify output before changing release facts.

Next action: refresh Graphify, then commit and push the validated migration on a purpose branch without absorbing the pre-existing untracked `.agents/`, `.claude/`, root `AGENTS.md`, or `data/release-5a-input/` content. Request an exact-SHA preview before updating the portfolio registry. Preserve the currently approved legacy SHA until the replacement is reviewed.

Do not promote root HTTP reachability or retrieval/provenance metrics into an availability, confidentiality, legal-accuracy, or production-readiness claim.
